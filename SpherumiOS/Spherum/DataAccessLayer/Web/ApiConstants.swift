import Foundation

enum ApiConstants {
    static let proto = "http"
    static let domain = "176.112.192.23"
    
    static let host = "\(proto)://\(domain)"
    
    static let apiBaseEndpoint = "\(host)/rest"
}
